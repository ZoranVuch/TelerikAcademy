using System;

    class HexadecimalFormat
    {
        static void Main()
        {
            int hexDecimalVariable = 0xFE;  // "FE" in hexadecimal is 254 in decimal

            Console.WriteLine(hexDecimalVariable);
        }
    }

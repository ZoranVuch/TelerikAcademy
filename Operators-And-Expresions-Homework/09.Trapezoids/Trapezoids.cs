using System;

    class Trapezoids
    {
        static void Main()
        {
            //define the variables
            double a, b, h, area;

       //example 1
            //assign values to variables
            a = 5;
            b = 7;
            h = 12;

            //calculate the area of the trapezod
            area = (a+b)*h/2;

            //print the result
            Console.WriteLine("Area of trapezoid with a={0}, b={1} and h={2} is: {3}.\n", a, b, h, area);

      //example 2
            //assign values to variables
            a = 2;
            b = 1;
            h = 33;

            //calculate the area of the trapezod
            area = (a + b) * h / 2;

            //print the result
            Console.WriteLine("Area of trapezoid with a={0}, b={1} and h={2} is: {3}.\n", a, b, h, area);


     //example 3
            //assign values to variables
            a = 8.5;
            b = 4.3;
            h = 2.7;

            //calculate the area of the trapezod
            area = (a + b) * h / 2;

            //print the result
            Console.WriteLine("Area of trapezoid with a={0}, b={1} and h={2} is: {3}.\n", a, b, h, area);


    //example 4
            //assign values to variables
            a = 100;
            b = 200;
            h = 300;

            //calculate the area of the trapezod
            area = (a + b) * h / 2;

            //print the result
            Console.WriteLine("Area of trapezoid with a={0}, b={1} and h={2} is: {3}.\n", a, b, h, area);


      //example 5
            //assign values to variables
            a = 0.222;
            b = 0.333;
            h = 0.555;

            //calculate the area of the trapezod
            area = (a + b) * h / 2;

            //print the result
            Console.WriteLine("Area of trapezoid with a={0}, b={1} and h={2} is: {3}.\n", a, b, h, area);

        }
    }
